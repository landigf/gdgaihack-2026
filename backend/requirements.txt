fastapi==0.115.5
# Required at FastAPI import time by any router that declares
# File / Form / UploadFile (we use it for the voice round-trip in
# backend/ares/router.py). FastAPI doesn't pull it in transitively —
# leaving it implicit means uvicorn dies on startup with a RuntimeError
# before serving the first request.
python-multipart==0.0.28
uvicorn[standard]==0.32.1
httpx==0.28.1
pydantic==2.10.3
faiss-cpu==1.9.0.post1
numpy==2.1.3
pypdf==5.1.0
python-docx==1.1.2
openpyxl==3.1.5
tiktoken==0.8.0
pytest==8.3.4
pytest-asyncio==0.24.0

# Houston tile-lattice cache (Arrow/Parquet) feeds /ares/sensor/query.
pyarrow>=17.0
# Houston perf endpoint snapshots for /ares/perf + benchmarks/houston/.
psutil>=5.9

# MLX backend — Apple Silicon only. PEP 508 markers skip these on every
# other platform, so requirements.txt stays universal.
mlx>=0.16; sys_platform == "darwin" and platform_machine == "arm64"
mlx-lm>=0.18; sys_platform == "darwin" and platform_machine == "arm64"
