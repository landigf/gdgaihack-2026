# /track:msi

Follow this workflow:
1. Read `explainit/gdg-ai-hack-2026/README.md` and `explainit/gdg-ai-hack-2026/msi-on-device-ai.md` first.
2. Ground the output in the current repo and the MSI On-Device AI track.
3. Create or update `doc/specs/<slug>/01-brainstorm.md` with 3-5 concrete local-first AI ideas and a recommended direction.
4. Bias toward privacy, low-latency, and unreliable-network scenarios instead of cloud-dependent demos.

If a required document is missing, create it from the matching template in `doc/specs/_templates/`.
