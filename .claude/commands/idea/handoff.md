# /idea:handoff

Follow this workflow:
1. Read `01-03` docs and produce `04-implementation.md` as an implementation handoff.
2. Summarize the target behavior, likely file areas, test ideas, and demo script.
3. Call out which parts need a stronger `deep` runtime review before coding.

If a required document is missing, create it from the matching template in `doc/specs/_templates/`.
